#include <stdio.h>

int main(){
    float perimeter, area;
    const float pi = 3.14;
    float r = 7.0;

    perimeter = (2 * pi * r);

    printf("The perimeter of the circle is : %f \n", perimeter);

    area = (pi * r * r);

    printf("The area of the circle is %f \n", area);

    return 0;
}
