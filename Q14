#include <stdio.h>

int main(){
    int a, b, i;

    printf("Enter the beginning of the range: ");
    scanf("%d", &a);

    printf("Enter the end of the range: ");
    scanf("%d", &b);

    for(i = a; i < b; i++){
        if(i % 3 == 0 || i % 5 == 0){
            printf("%d\n", i);

        }
    }

    return 0;
}
