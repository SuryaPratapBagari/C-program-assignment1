#include <stdio.h>

int main(){
    int i, a, b;

    printf("Enter the starting number: ");
    scanf("%d", &a);

    printf("Enter the ending number: ");
    scanf("%d", &b);
    
    for(i=a; i<b; i++){
        if(i % 2 != 0){
            printf("%d\n", i);
        }
    }
    return 0;
}
